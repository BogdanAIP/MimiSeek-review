# Reports

Human-readable/generated views of canonical MimiSeek data live here.

The existing Excel reviewer-statistics workbook is the bootstrap human report and import source. After canonical text datasets exist, future workbooks/dashboards should be generated from those datasets rather than manually becoming a second source of truth.
