# MimiSeek Review

Standalone, reusable code-review system for multiple repositories.

The project is being bootstrapped. Development state and governing documents are added through pull requests; `main` is the stable branch.
